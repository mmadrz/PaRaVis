"""
paravis.gui — PySide6 desktop application layer.

Sub-packages:
    gui.widgets      — Main panel widgets (Indices, Rao's Q, Visualization)
    gui.dialogs      — Dialog windows (About, Settings, Band Mapping, ...)
    gui.models       — Qt item models and delegates
    gui.components   — Reusable UI components (Splash, RecentFiles, MplCanvas, Workers)
    gui.theme        — QSS stylesheets
"""
